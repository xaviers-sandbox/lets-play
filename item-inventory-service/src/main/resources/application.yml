spring:
  application:
    name: item-inventory-service
  profiles:
    active: local

server:
  port: 1111  

---
spring:
  config:
    activate:
      on-profile: local      
  data:
    mongodb:
      host: localhost
      port: 27017
      database: local

logging:
  level: 
    "[com.item.inventory]" : DEBUG         
  
---
spring:
  config:
    activate:
      on-profile: test       
  data:
    mongodb:
      host: localhost
      port: 27017
      database: local
logging:
  level: 
    "[com.item.inventory]" : DEBUG     
